pvsio : THEORY
BEGIN

  IMPORTING structures@Unit,
	    fast_approx,
	    reals@sqrt,
            trig@sincos_def,
	    trig@pi_def,
	    trig@atan,
	    lnexp@ln_exp

  %% This constant is used by PVSio to check if all the required theories 
  %% have been imported in the current context
  FastApproxIO__ : Unit

END pvsio
