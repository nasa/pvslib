top : THEORY
BEGIN
  
  IMPORTING PVS0,
  	    BoolPVS0,
	    lang,
	    eval_props,
	    measure_termination_defs,
	    pvs0_to_dg,
	    measure_termination,
	    scp_iff_pvs0,
	    ccg_to_pvs0,
	    % Examples (to be proven)
            ack_ax, ack_pvs0,
	    factorial_ax, factorial_pvs0,
	    gcd_ax, gcd_pvs0
	    	    
END top
