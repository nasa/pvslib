factorial_pvs  % [ parameters ]
		: THEORY

  BEGIN

  IMPORTING factorial_pvs0

  factorial(n:nat): RECURSIVE posnat =
 	      IF n=0 THEN 1
 	      ELSE N*factorial(n-1)
 	      ENDIF
  MEASURE factorial_wfm BY <

  END factorial_pvs
