%
% Properties valid on the metric_def[real] theory.
%
metric_def_real_props: THEORY
BEGIN

  IMPORTING metric_def[real]
  
  metric?_abs: LEMMA
    metric?((LAMBDA (x, y: real): abs(x - y)))
    
END metric_def_real_props
