% This theory enables a conversion from array literals into finseq (see arrays_examples)

arraysn[N:nat,T:TYPE] : THEORY
BEGIN

  IMPORTING arrays

  array2finseq(a:ArrayOf[N][T]) : finseq[T] = (#
    length := N,
    seq    := a
  #)

  CONVERSION array2finseq

END arraysn
