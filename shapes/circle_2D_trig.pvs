circle_2D_trig : THEORY
BEGIN

    IMPORTING circle_2D,
              circular_segment_2D_trig

    perimeter(CC:Circle_2D) : posreal =
      2*pi*radius(CC)

    area(CC:Circle_2D) : posreal =
      pi*sq(radius(CC))  

END circle_2D_trig