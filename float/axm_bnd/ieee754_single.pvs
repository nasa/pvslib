ieee754_single: THEORY
BEGIN

  IMPORTING ieee754_single_base
  
  ;< (x,y: binary32): bool = qlt_ieee754(x,y)
  ;<=(x,y: binary32): bool = qle_ieee754(x,y)
  ;> (x,y: binary32): bool = qgt_ieee754(x,y)
  ;>=(x,y: binary32): bool = qge_ieee754(x,y)
  
  % #TODO check if we want to keep this; the problem is that we lose
  %       the replace by equals.
  %   ;= (x,y: binary32): bool = qeq_ieee754(x,y)

END ieee754_single
