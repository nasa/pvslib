interface_trig: THEORY
BEGIN

     % IMPORTING
     % 	      trig_fnd@trig_basic, trig_fnd@trig_ineq, trig_fnd@atan, trig_fnd@atan2,
     %        trig_fnd@integral_indef_sincos,
     %        trig_fnd@atan_values,  
     % 	      trig_fnd@trig_inverses

     IMPORTING
              trig@trig_basic, trig@trig_ineq, trig@atan, trig@atan2,
              trig@integral_indef_sincos,
              trig@atan_values,  
     	      trig@trig_inverses

 
END interface_trig


