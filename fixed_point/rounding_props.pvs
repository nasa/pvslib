rounding_props
[base: above(1)
, (importing fixed_point_format[base])
  format: fixed_point_format
, round: [real->int]]: THEORY
BEGIN

  importing basic_defs[base,format,round]

  % bounds for representation error of the conversion function from fixed_point to real based on the given round
  round_error: type = {r: real | ∀(x: (in_range?)): abs(round(x) - x) <= r}

END rounding_props
