accumulated_error_inst
[ base: above(1)
, (importing fixed_point_format[base])
  format: fixed_point_format ] : THEORY
BEGIN

  importing roundings[base,format]
  importing accumulated_error
  
  % rounding to nearest (even)

  err_mul_rne: [real,real,nonneg_real,nonneg_real -> nonneg_real]
  = err_mul[base,format,round_to_nearest_even,round_to_nearest_even_bound]

END accumulated_error_inst
